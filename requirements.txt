

# Core scientific stack
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
scikit-image>=0.21.0
networkx>=3.1

# Deep learning - CPU-only wheel via the index above.
# Keeps the install ~200MB instead of ~2.5GB with CUDA libraries.
torch>=2.12.1
einops>=0.7.0

# Image I/O for the synthetic-demo generator and uploaded-image path
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# Dashboard
streamlit>=1.28.0
plotly>=5.17.0

# Config
pyyaml>=6.0
